#!/bin/sh
# Cache-Busting für alle HTML-Dateien vor jedem Commit.

HASH=$(git rev-parse --short HEAD 2>/dev/null || echo "000000")
for f in index.html anreise.html intern.html intern-bereich.html; do
  [ -f "$f" ] && sed -i "s/?v=[a-f0-9]*\"/?v=$HASH\"/g" "$f" && git add "$f"
done

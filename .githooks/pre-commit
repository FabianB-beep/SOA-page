#!/bin/sh
# Setzt den Cache-Busting-Hash in index.html vor jedem Commit.
# Verwendet den Hash des aktuellen HEAD (letzter Commit) – reicht für Cache Busting.

HASH=$(git rev-parse --short HEAD 2>/dev/null || echo "000000")
sed -i "s/?v=[a-f0-9]*\"/?v=$HASH\"/g" index.html
git add index.html
